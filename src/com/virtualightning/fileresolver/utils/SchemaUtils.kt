package com.virtualightning.fileresolver.utils

import javax.xml.validation.Schema


fun runFormatSchema(format : String) {
    var schema : Schema? = null

    var flagStr = ""
    format.chars().forEach {

    }
}